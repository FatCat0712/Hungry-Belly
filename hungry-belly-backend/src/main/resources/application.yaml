spring:
  application:
    name: hungry-belly-backend

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/foodies}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Maisonhai@789}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  output:
    ansi:
      enabled: ALWAYS

